<%= l(:text_issue_added, :id => "##{@issue.id}", :author => @issue.author) %>

----------------------------------------
<%= render :partial => "issue.text.erb", :locals => { :issue => @issue, :issue_url => @issue_url } %>

<%= "Have a nice day." if @issue.author.is_vip -%>
